# February 17 - Alpha Rarefaction on QIIME2

# Generate a tree for phylogenetic diversity analyses
qiime phylogeny align-to-tree-mafft-fasttree \
  --i-sequences rep-seqs.qza \
  --o-alignment aligned-rep-seqs.qza \
  --o-masked-alignment masked-aligned-rep-seqs.qza \
  --o-tree unrooted-tree.qza \
  --o-rooted-tree rooted-tree.qza 

# Alpha-rarefaction
qiime diversity alpha-rarefaction \
  --i-table table.qza \
  --i-phylogeny rooted-tree.qza \
  --p-max-depth 32000 \
  --m-metadata-file /data/project_2/metadata_usa.tsv \
  --o-visualization alpha-rarefaction.qzv

# Secure transfer to computer
scp -r root@10.19.139.184:/data/project_2/alpha-rarefaction.qzv .

# Visualize rarefaction curve using QIIME2 view

# This rarefaction curve did not provide sufficient information because there were too many samples to visualize
# We will try to visualize the rarefaction curve using R instead

